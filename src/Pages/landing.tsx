import React from "react";
import ScrollableFeed from "react-scrollable-feed";
import Decorator from "../mini-components/decorator";

const Landing = () => {
  return (
    <div className="w-screen h-screen">
      <Decorator />
    </div>
  );
};

export default Landing;
